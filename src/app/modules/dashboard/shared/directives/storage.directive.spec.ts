import { StorageDirective } from './storage.directive';

describe('StorageDirective', () => {
  it('should create an instance', () => {
    const directive = new StorageDirective();
    expect(directive).toBeTruthy();
  });
});
